
/**
 * Arrows on top, bottom, left, right to change rooms (waypoints)
 * Will change rooms depending on arrow direction selected
 */

function Navigation(){
    return(
        console.log("Navigation")
        )  
}

export default Navigation; 